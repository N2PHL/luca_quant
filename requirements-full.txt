# =============================================================
# L.U.C.A Quant — requirements cho BẢN ĐẦY ĐỦ (chạy local)
# profile `full` — toàn bộ 34 mô hình / 6 họ
#
#     pip install -r requirements-full.txt
#     pytest
#     streamlit run app.py
#
# KHÔNG dùng file này để deploy Streamlit Community Cloud — tổng dung lượng
# vượt xa ngân sách của free tier. Bản deploy dùng requirements.txt.
# =============================================================

# --- Bắt buộc ---
streamlit>=1.40.0,<2.0.0
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<3.0.0
scipy>=1.11.0
scikit-learn>=1.3.0
plotly>=5.18.0
requests>=2.31.0

# --- Deep Learning: 10 kiến trúc chuỗi ---
# rnn | lstm | gru | bilstm | bigru | cnn1d | tcn | cnn_lstm | attn_lstm
# | transformer
# Bản CPU (~200MB) là đủ; dữ liệu ngày chỉ vài nghìn phiên nên GPU gần như
# không giúp gì, phần lớn thời gian nằm ở overhead Python chứ không ở phép
# nhân ma trận.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0

# --- Gradient boosting ---
lightgbm>=4.3.0
# CatBoost ~150MB, XGBoost ~250MB. Registry tự nhận diện, bỏ comment nếu cần.
# catboost>=1.2.5
# xgboost>=2.0.0

# --- Econometric: ARIMA / GARCH ---
# garch_vol là baseline vol-timing KHÓ VƯỢT — rất đáng có trong bảng.
statsmodels>=0.14.0
arch>=6.3.0

# --- Dữ liệu tuỳ chọn ---
# yfinance chỉ dùng cho benchmark quốc tế; DNSE là nguồn chính.
# yfinance>=0.2.36

# --- Kiểm định ---
pytest>=8.0.0
