# =============================================================
# L.U.C.A Quant — requirements cho BẢN NỘP TRỰC TUYẾN
# (profile `coursework`, deploy trên Streamlit Community Cloud)
#
# Chỉ 6 kiến trúc đề bài yêu cầu: MLP, CNN, RNN, LSTM, GRU, Transformer.
# Chạy đầy đủ 34 mô hình ở máy local -> dùng requirements-full.txt
# =============================================================

# --- Bắt buộc ---
streamlit>=1.40.0,<2.0.0
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<3.0.0
scipy>=1.11.0
scikit-learn>=1.3.0
plotly>=5.18.0
requests>=2.31.0

# --- PyTorch: BẢN CPU ---
# Dòng --extra-index-url PHẢI đứng trước `torch`.
#
# Không có nó, pip lấy wheel torch mặc định trên PyPI — wheel đó kéo theo
# toàn bộ runtime CUDA (~2GB). Streamlit Community Cloud giới hạn khoảng 1GB
# RAM, nên build sẽ FAIL, không phải chạy chậm. Bản CPU chỉ ~200MB.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0

# =============================================================
# CỐ Ý KHÔNG CÀI — đây là lý do bản online build được
# =============================================================
# lightgbm / catboost / xgboost : profile `coursework` không dùng gradient
#     boosting. Riêng CatBoost ~150MB và XGBoost ~250MB, cộng vào là hết ngân
#     sách dành cho torch.
# statsmodels / arch           : ARIMA và GARCH thuộc họ econometric, không
#     nằm trong phạm vi đề bài phần này.
# yfinance                     : DNSE là nguồn dữ liệu chính.
# pytest                       : chỉ chạy local.
#
# Registry tự bỏ mô hình khi thiếu thư viện, app KHÔNG sập.
